# Render Blueprint Configuration
# Documentation: https://docs.render.com/blueprint-spec

services:
  # ============================================
  # Backend API
  # ============================================
  - type: web
    name: ds-proforma-api
    runtime: node
    plan: free  # Change to 'starter' for production
    region: oregon
    rootDir: backend
    buildCommand: npm install && npx prisma generate && npx prisma migrate deploy
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: ds-proforma-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: CORS_ORIGIN
        sync: false  # Set manually in dashboard

  # ============================================
  # Frontend App (React)
  # Static sites don't need a plan - they're free
  # ============================================
  - type: web
    name: ds-proforma-frontend
    runtime: static
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    headers:
      - path: /*
        name: Cache-Control
        value: public, max-age=0, must-revalidate
      - path: /assets/*
        name: Cache-Control
        value: public, max-age=31536000, immutable
    routes:
      - type: rewrite
        source: /*
        destination: /index.html

  # ============================================
  # Static Sites - Add your landing pages here
  # ============================================
  
  # Example Landing Page (rename/duplicate for each site)
  - type: web
    name: example-landing
    runtime: static
    rootDir: sites/example-landing
    staticPublishPath: .
    headers:
      - path: /*
        name: Cache-Control
        value: public, max-age=3600
      - path: /css/*
        name: Cache-Control
        value: public, max-age=31536000, immutable
      - path: /js/*
        name: Cache-Control
        value: public, max-age=31536000, immutable
      - path: /images/*
        name: Cache-Control
        value: public, max-age=31536000, immutable

  # ============================================
  # TEMPLATE: Copy this block for new sites
  # ============================================
  # - type: web
  #   name: your-site-name
  #   runtime: static
  #   rootDir: sites/your-site-folder
  #   staticPublishPath: .
  #   headers:
  #     - path: /*
  #       name: Cache-Control
  #       value: public, max-age=3600
  #     - path: /css/*
  #       name: Cache-Control
  #       value: public, max-age=31536000, immutable
  #     - path: /js/*
  #       name: Cache-Control
  #       value: public, max-age=31536000, immutable
  #     - path: /images/*
  #       name: Cache-Control
  #       value: public, max-age=31536000, immutable

# ============================================
# Database
# NOTE: Your existing database is on basic-256mb plan
# This matches your current Render setup
# ============================================
databases:
  - name: ds-proforma-db
    plan: basic_256mb
    region: oregon
    databaseName: ds_proforma
    user: ds_proforma_user
